#ifndef MH

#define MH

SCI_MENU * menu_guest();
SCI_MENU * menu_login();
SCI_MENU * menu_staff();
SCI_MENU * menu_admin();
SCI_MENU * menu_admin_select(UINT);
SCI_MENU * menu_admin_edit(UINT);

#endif